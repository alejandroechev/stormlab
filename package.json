{
  "name": "stormlab",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @stormlab/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test:watch",
    "lint": "pnpm -r lint"
  }
}
